var tiempo, costo, multiplicar;

tiempo = prompt("Por favor, ingresa el tiempo por minuto");
costo = prompt("Por favor, ingresa el costo por minuto ");

resultado = parseFloat(tiempo) * parseFloat(costo);

alert(resultado);